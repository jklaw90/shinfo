apiVersion: v1
kind: Service
metadata:
  name: {{ include "shinfo.fullname" . }}
  labels:
    {{- include "shinfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shinfo.selectorLabels" . | nindent 4 }}
