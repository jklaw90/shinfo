apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shinfo.fullname" . }}-test-connection"
  labels:
    {{- include "shinfo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shinfo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
